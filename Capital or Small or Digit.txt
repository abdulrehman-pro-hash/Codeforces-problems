#include <iostream>
using namespace std;
int main() {
  char ch;
  cin>>ch;
  if (ch=='A' && ch <= 'Z')
     cout<< "ALPHA IS CAPITAL";
  else if (ch=='a' && ch <= 'z')
    cout<< "ALPHA IS SMALL";
  else
    cout<<"ALPAH IS DIGIT";
  
    return 0;
}